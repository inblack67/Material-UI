import { Button, Container } from '@material-ui/core';

const Buttons = () =>
{
    return (
        <Container>
            <Button variant='contained' color='secondary' fullWidth>Hello Friend</Button>
        </Container>
    );
};

export default Buttons;
